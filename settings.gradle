plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'hamcrest-auto-matcher'

include ':latest-java-tests'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
